name: Keep Render awake
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: curl -fsS --max-time 40 https://mnb-omni-caller.onrender.com/api/me > /dev/null || true
